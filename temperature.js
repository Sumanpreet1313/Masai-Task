// Create an array to store temperatures recorded over 7 days
let temperatures = [30, 32, 31, 29, 33, 34, 35];

// Access the temperature of the 4th day (index 3) and update it
temperatures[3] += 2;

// Log the updated temperatures to the console
console.log(temperatures);
